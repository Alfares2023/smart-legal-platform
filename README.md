💻  منصة تحليل العقود الذكية (Streamlit/Firestore Architecture)

1. بنية التطبيق (Application Architecture)

يعتمد التطبيق على بنية متكاملة أحادية الملف (Single-File Monolithic) باستخدام إطار عمل Streamlit، مع فصل واضح بين طبقة العرض والتعامل مع البيانات المستمر (Persistence Layer).

الطبقة التقنية

المكون الأساسي

المسؤولية الرئيسية

واجهة المستخدم والعرض (UI/Presentation)

Streamlit (Python)

إدارة تدفق التطبيق، تفاعلات المستخدم (Forms, Tabs)، وعرض نتائج التحليل وقاعدة البيانات في الوقت الفعلي.

منطق الأعمال (Business Logic)

Python Functions

محاكاة عمليات التحليل اللغوي (NLP Mockup) وتوليد نصوص العقود.

طبقة البيانات المستمرة (Persistence Layer)

Google Cloud Firestore

تخزين طلبات المستخدمين (Manual Records) في مسار خاص ومؤمن (artifacts/{appId}/users/{userId}/...).

2. آليات تكامل البيانات والـ Firestore

يتم إدارة الاتصال بقاعدة البيانات عبر الخطوات التالية:

التهيئة (Initialization): يتم استخدام الدالة setup_firebase() لتحليل متغيرات البيئة (__firebase_config) وتهيئة خدمات firebase-admin و firestore.client().

تحديد المسار (Pathing): يتم تحديد مسار التخزين الخاص بكل مستخدم ديناميكياً باستخدام المعرفات البيئية (__app_id و user_id المُشتق من __initial_auth_token).

الكتابة (Write Operations): تتم عمليات إرسال بيانات الطلبات اليدوية عبر التفاعل المباشر مع ميثود db.collection(COLLECTION_PATH).add(record_data).

القراءة في الوقت الفعلي (Real-time Read - Mandatory): يتم استخدام وظيفة on_snapshot لإنشاء مستمع دائم (Listener) على مجموعة بيانات المستخدم. هذه الآلية تضمن:

جلب البيانات عند التحميل الأولي.

تحديث حالة التطبيق (st.session_state.records) وإعادة تشغيل واجهة المستخدم (st.experimental_rerun()) في كل مرة يحدث فيها تغيير في قاعدة البيانات.

3. قدرات المنطق (Business Capabilities)

Contract Analysis Mockup: دالة Python ( analyze_contract ) تحاكي عملية تحديد الكلمات المفتاحية والبنود (مثل الشرط الجزائي أو الإيجار) ضمن نص العقد المُدخل.

Contract Generation Mockup: دالة Python ( generate_contract ) تُنشئ نص نموذج عقد بناءً على معايير مُدخلة (الموضوع والصنف).
